# ~/.tmux.conf

set -g default-terminal "screen-256color"
set -ga terminal-overrides ",*256col*:Tc"

set -s escape-time 10                     # faster command sequences
set -s set-clipboard on

set -q -g status-utf8 on                  # expect UTF-8 (tmux < 2.2)
setw -q -g utf8 on

set -g history-limit 5000                 # boost history

set -g base-index 1           # start windows numbering at 1
setw -g pane-base-index 1     # make pane numbering consistent with windows

setw -g automatic-rename on   # rename window to reflect current program
set -g renumber-windows on    # renumber windows when a window is closed

set -g set-titles on          # set terminal title

set -g display-panes-time 800 # slightly longer pane indicators display time
set -g display-time 1000      # slightly longer status messages display time

set -g status-interval 10     # redraw status line every 10 seconds

# activity
set -g monitor-activity on
set -g visual-activity off

# Send prefix
set-option -g prefix C-a
bind-key C-a send-prefix

# Set easier window split keys
bind-key v split-window -h
bind-key h split-window -v

# -- navigation ----------------------------------------------------------------

# create session
bind C-c new-session

# find session
bind C-f command-prompt -p find-session 'switch-client -t %%'

# session navigation
bind BTab switch-client -l  # move to last session

# split current window horizontally
bind v split-window -v
# split current window vertically
bind s split-window -h

# pane navigation
bind h select-pane -L  # move left
bind j select-pane -D  # move down
bind k select-pane -U  # move up
bind l select-pane -R  # move right
bind > swap-pane -D       # swap current pane with the next one
bind < swap-pane -U       # swap current pane with the previous one

# maximize current pane
bind + run "cut -c3- '#{TMUX_CONF}' | sh -s _maximize_pane '#{session_name}' '#D'"

# window navigation
bind p previous-window        # move to last active window
bind n next-window        # move to last active window


# Easy config reload
bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded."

# Copying from vim to tmux
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi v send -X begin-selection
bind-key -T copy-mode-vi V send -X select-line
bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# Mouse mode
setw -g mouse on

# Status bar color

# # ~/.tmux.conf
#
# set -g default-terminal "screen-256color"
# set -ga terminal-overrides ",*256col*:Tc"
#
# set -s escape-time 10                     # faster command sequences
# set -s set-clipboard on                   # OSC 52 -> schowek przez WezTerm
# set -g focus-events on                    # nvim dostaje focus in/out (autoread)
#
# set -q -g status-utf8 on                  # expect UTF-8 (tmux < 2.2)
# setw -q -g utf8 on
#
# set -g history-limit 5000                 # boost history
#
# set -g base-index 1           # start windows numbering at 1
# setw -g pane-base-index 1     # make pane numbering consistent with windows
#
# setw -g automatic-rename on   # rename window to reflect current program
# set -g renumber-windows on    # renumber windows when a window is closed
#
# set -g set-titles on          # set terminal title
#
# set -g display-panes-time 800 # slightly longer pane indicators display time
# set -g display-time 1000      # slightly longer status messages display time
#
# set -g status-interval 10     # redraw status line every 10 seconds
#
# # activity
# set -g monitor-activity on
# set -g visual-activity off
#
# # Send prefix
# set-option -g prefix C-a
# bind-key C-a send-prefix
#
# # -- navigation ----------------------------------------------------------------
#
# # create session
# bind C-c new-session
#
# # find session
# bind C-f command-prompt -p find-session 'switch-client -t %%'
#
# # session navigation
# bind BTab switch-client -l  # move to last session
#
# # split jak w wezterm: v = obok (prawo), s = pod spodem
# bind v split-window -h -c "#{pane_current_path}"
# bind s split-window -v -c "#{pane_current_path}"
#
# # nowe okno (jak nowy tab w wezterm), w katalogu biezacego pane
# bind c new-window -c "#{pane_current_path}"
#
# # pane navigation
# bind h select-pane -L  # move left
# bind j select-pane -D  # move down
# bind k select-pane -U  # move up
# bind l select-pane -R  # move right
# bind > swap-pane -D       # swap current pane with the next one
# bind < swap-pane -U       # swap current pane with the previous one
#
# # zamknij pane / okno bez pytania (jak x / X w wezterm)
# bind x kill-pane
# bind X kill-window
#
# # adjust pane size (strzalki, powtarzalne bez ponownego prefixu)
# bind -r Left  resize-pane -L 1
# bind -r Right resize-pane -R 1
# bind -r Up    resize-pane -U 1
# bind -r Down  resize-pane -D 1
#
# # move panes: wybierz numer pane do zamiany / przenies pane do nowego okna
# bind w display-panes -d 0 "swap-pane -t '%%'"
# bind W display-panes -d 0 "swap-pane -d -t '%%'"
# bind m break-pane
#
# # window navigation
# bind p previous-window
# bind n next-window
#
# # move windows (jak P / N w wezterm)
# bind -r P swap-window -d -t -1
# bind -r N swap-window -d -t +1
#
# # Easy config reload
# bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded."
#
# # Copying from vim to tmux
# set-window-option -g mode-keys vi
# bind-key -T copy-mode-vi v send -X begin-selection
# bind-key -T copy-mode-vi V send -X select-line
# bind-key -T copy-mode-vi y send -X copy-selection-and-cancel
#
# # Mouse mode
# setw -g mouse on

